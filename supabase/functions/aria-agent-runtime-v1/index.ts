import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const URL=Deno.env.get("SUPABASE_URL")!;const KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;const SECRET=Deno.env.get("ARIA_RUNTIME_SHARED_SECRET")??"";const EXEC=`${URL}/functions/v1/aria-execution-runtime-v1`;const sb=createClient(URL,KEY,{auth:{persistSession:false,autoRefreshToken:false}});const out=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{"content-type":"application/json","cache-control":"no-store"}});const eq=(a:string,b:string)=>{const x=new TextEncoder().encode(a),y=new TextEncoder().encode(b);if(x.length!==y.length)return false;let d=0;for(let i=0;i<x.length;i++)d|=x[i]^y[i];return d===0};const token=(r:Request)=>{const h=r.headers.get("authorization")??"";return h.startsWith("Bearer ")?h.slice(7):r.headers.get("x-aria-autonomy-token")};async function auth(r:Request){const t=token(r);if(t&&SECRET&&eq(t,SECRET))return true;if(!t)return false;const {data,error}=await sb.rpc("aria_autonomy_cron_authorize",{p_token:t});return !error&&data===true}const agents:Record<string,{model_id:string;system:string}>={"aria-agent-planner-v1":{model_id:"google/gemini-2.5-flash-lite",system:"You are ARIA's planning specialist agent. Return concise, actionable reasoning only."},"aria-agent-reviewer-v1":{model_id:"google/gemini-2.5-flash-lite",system:"You are ARIA's verification specialist agent. Critically review the supplied task and return concrete findings only."}};
Deno.serve(async r=>{if(r.method!=="POST")return out({error:"method_not_allowed"},405);if(!(await auth(r)))return out({error:"unauthorized"},401);const b:any=await r.json().catch(()=>({}));const agentId=String(b.agent_id||"");const a=agents[agentId];if(!a)return out({ok:false,status:"blocked",error:{code:"agent_not_registered"}},200);if(String(b.operation||"delegate")!=="delegate")return out({ok:false,status:"blocked",error:{code:"operation_not_supported"}},200);const missionId=String(b.mission_id||"");const stepId=String(b.step_id||"");const requestId=`agent:${missionId}:${stepId}`;const prompt=typeof b.input?.prompt==="string"?b.input.prompt.trim():String(b.input?.message||"").trim();if(!prompt)return out({ok:false,status:"blocked",error:{code:"prompt_missing"}},200);const route={status:"selected",provider_id:"openrouter",account_id:"acct_openrouter_primary",model_id:a.model_id,capability:"text_generation"};const h={"content-type":"application/json",authorization:`Bearer ${SECRET}`};const rr=await fetch(EXEC,{method:"POST",headers:h,body:JSON.stringify({execution_version:"1",request_id:requestId,task_id:stepId,capability:"text_generation",selected_route:route,authorization:{status:"approved",risk_class:String(b.risk||"READ"),evidence_ref:`agent:${agentId}`},input:{payload:{prompt:`${a.system}\n\nTask:\n${prompt}`}},policy:b.policy||{},metadata:{executor_type:"agent",agent_id:agentId,mission_id:missionId,step_id:stepId}})});const x:any=await rr.json().catch(()=>null);if(!rr.ok||x?.status!=="succeeded")return out({ok:false,status:"failed",agent_id:agentId,executor_type:"agent",error:{code:"agent_execution_failed",message:String(x?.error?.message||x?.error||`execution_${rr.status}`)}},200);return out({ok:true,status:"succeeded",executor_type:"agent",agent_id:agentId,operation:"delegate",provider_id:route.provider_id,model_id:route.model_id,response:x.response,usage:x.usage,metadata:{mission_id:missionId,step_id:stepId}});});