import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const V="aria-mission-runner-v22-universal";const URL=Deno.env.get("SUPABASE_URL")!;const KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;const SECRET=Deno.env.get("ARIA_RUNTIME_SHARED_SECRET")??"";const MEMORY=`${URL}/functions/v1/aria-memory-v2`;const PLANNER=`${URL}/functions/v1/aria-planner-v11`;const EXEC=`${URL}/functions/v1/aria-execution-runtime-v1`;const RUNTIME=`${URL}/functions/v1/aria-runtime-gateway-v1`;const sb=createClient(URL,KEY,{auth:{persistSession:false,autoRefreshToken:false,autoRefreshSession:false}});const out=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{"content-type":"application/json","cache-control":"no-store"}});const eq=(a:string,b:string)=>{const x=new TextEncoder().encode(a),y=new TextEncoder().encode(b);if(x.length!==y.length)return false;let d=0;for(let i=0;i<x.length;i++)d|=x[i]^y[i];return d===0};const tokenOf=(r:Request)=>{const h=r.headers.get("authorization")??"";return h.startsWith("Bearer ")?h.slice(7):r.headers.get("x-aria-autonomy-token")};const headersFor=(token:string|null)=>{const h:Record<string,string>={"content-type":"application/json"};if(token)h["x-aria-autonomy-token"]=token;else if(SECRET)h.authorization=`Bearer ${SECRET}`;return h};const internalHeaders=()=>({"content-type":"application/json",authorization:`Bearer ${SECRET}`});const rpc=async(name:string,args:Record<string,unknown>)=>{const {data,error}=await sb.rpc(name,args);if(error)throw new Error(`${name}:${error.message}`);return data};async function auth(r:Request){const token=tokenOf(r);if(token&&SECRET&&eq(token,SECRET))return true;if(!token)return false;const {data,error}=await sb.rpc("aria_autonomy_cron_authorize",{p_token:token});return !error&&data===true}async function recall(goal:string,token:string|null){try{const r=await fetch(MEMORY,{method:"POST",headers:headersFor(token),body:JSON.stringify({action:"search",query:goal,limit:8})});const b=await r.json().catch(()=>null);return{available:r.ok&&b?.ok===true,results:Array.isArray(b?.results)?b.results:[]}}catch{return{available:false,results:[]}}}async function plan(goal:string,context:unknown,token:string|null){const r=await fetch(PLANNER,{method:"POST",headers:headersFor(token),body:JSON.stringify({goal,context})});const b=await r.json().catch(()=>null);if(!r.ok||!b?.ok||!Array.isArray(b.plan?.steps))throw new Error(`planner_${r.status}`);return b.plan.steps}async function emit(id:string,event_type:string,payload:unknown){await rpc("aria_mission_append_event",{p_mission_id:id,p_event:{event_type,payload}})}async function update(id:string,patch:Record<string,unknown>){return await rpc("aria_mission_update",{p_mission_id:id,p_mission:patch})}function targetType(step:any){return String(step?.executor_type||step?.target?.type||"")}function validateStep(step:any){const type=targetType(step),operation=String(step?.operation||"");if(!["connector","device","model","agent"].includes(type))throw new Error(`unknown_executor_type:${type}`);if(!operation)throw new Error("operation_missing");if(type==="connector"&&!step.target?.connector_id)throw new Error("connector_target_missing");if(type==="device"&&!step.target?.device_id)throw new Error("device_target_missing");if(type==="model"&&(!step.target?.provider_id||!step.target?.account_id||!step.target?.model_id))throw new Error("model_route_incomplete");if(type==="agent"&&!step.target?.agent_id)throw new Error("agent_target_missing")}async function connectorExecute(id:string,step:any){const connector=String(step.target.connector_id),op=String(step.operation);if(connector==="supabase"&&(op==="health"||op==="mission_read"))return{status:"succeeded",executor_type:"connector",connector_id:connector,operation:op,data:op==="mission_read"?await rpc("aria_mission_get",{p_mission_id:id}):{ok:true}};if(connector==="cloudflare"&&(op==="health"||op==="worker_read"||op==="deployment_read")){const r=await fetch("https://aria.robvg9.workers.dev/",{headers:{"user-agent":`${V}-connector-probe`}});if(!r.ok)throw new Error(`cloudflare_unavailable_${r.status}`);return{status:"succeeded",executor_type:"connector",connector_id:connector,operation:op,http_status:r.status}}throw new Error(`connector_operation_not_allowed:${connector}:${op}`)}async function readJob(jobId:string){const r=await fetch(RUNTIME,{method:"POST",headers:internalHeaders(),body:JSON.stringify({action:"get_job",job_id:jobId})});const b=await r.json().catch(()=>null);return{r,b}}async function deviceExecute(id:string,step:any,attempt:number){const safeId=(v:string)=>v.replace(/[^a-zA-Z0-9_-]/g,"_").slice(0,24);const jobId=`uo_${safeId(id)}_${safeId(String(step.id))}_a${attempt}`;const terminal=["succeeded","failed","timeout","cancelled","blocked"];let current=await readJob(jobId);if(!(current.r.ok&&current.b?.ok&&current.b.job)){const req={action:"enqueue_device_job",job_id:jobId,mission_id:id,device_id:step.target.device_id,operation:"shell.execute",command:String(step.input?.command||"echo ARIA_UO_LIVE"),cwd:typeof step.input?.cwd==="string"?step.input.cwd:null,timeout_ms:Number.isInteger(step.timeout_ms)?step.timeout_ms:30000,policy:step.policy||{},metadata:{runner:V,executor_type:"device",attempt,idempotency_key:jobId}};const enq=await fetch(RUNTIME,{method:"POST",headers:internalHeaders(),body:JSON.stringify(req)});const b=await enq.json().catch(()=>null);if(!enq.ok||!b?.ok)throw new Error(`device_enqueue_${enq.status}`)}const deadline=Date.now()+18000;while(Date.now()<deadline){current=await readJob(jobId);const job=current.b?.job;const status=String(job?.status||"");if(current.r.ok&&current.b?.ok&&job&&terminal.includes(status))return{status,executor_type:"device",operation:"shell.execute",job_id:jobId,exit_code:job.exit_code,stdout:job.stdout,stderr:job.stderr};await new Promise(r=>setTimeout(r,500))}return{status:"waiting",executor_type:"device",operation:"shell.execute",job_id:jobId}}async function modelExecute(id:string,step:any,token:string|null){const route={status:"selected",provider_id:String(step.target.provider_id),account_id:String(step.target.account_id),model_id:String(step.target.model_id),capability:String(step.operation)};const authorization=step.authorization&&typeof step.authorization==="object"?step.authorization:{status:"approved",risk_class:step.risk||"READ",evidence_ref:`mission:${id}`};const r=await fetch(EXEC,{method:"POST",headers:headersFor(token),body:JSON.stringify({execution_version:"1",request_id:`${id}:${step.id}`,task_id:step.id,capability:String(step.operation),selected_route:route,authorization,input:step.input||{},policy:step.policy||{},metadata:{mission_id:id,step_id:step.id,executor_type:"model",runner:V}})});const b=await r.json().catch(()=>null);if(!r.ok||b?.status!=="succeeded")throw new Error(String(b?.error?.message||b?.error||`execution_${r.status}`));return{...b,executor_type:"model",operation:step.operation,provider_id:route.provider_id,account_id:route.account_id,model_id:route.model_id}}async function executeStep(id:string,step:any,attempt:number,token:string|null){validateStep(step);const type=targetType(step);if(type==="connector")return connectorExecute(id,step);if(type==="device")return deviceExecute(id,step,attempt);if(type==="model")return modelExecute(id,step,token);throw new Error(`agent_executor_unavailable:${String(step.target?.agent_id||"")}`)}function depsReady(step:any,done:Set<string>){return(Array.isArray(step?.depends_on)?step.depends_on:[]).every((d:any)=>done.has(String(d)))}function batch(steps:any[],done:Set<string>){const ready=steps.filter(s=>!done.has(String(s.id))&&depsReady(s,done));return ready.length>1&&ready.slice(0,2).every((s:any)=>String(s.risk||"READ").toUpperCase()==="READ"&&(targetType(s)==="connector"||targetType(s)==="model"))?ready.slice(0,2):ready.slice(0,1)}Deno.serve(async r=>{if(r.method!=="POST")return out({error:"method_not_allowed"},405);if(!(await auth(r)))return out({error:"unauthorized"},401);const body=await r.json().catch(()=>({}));const token=tokenOf(r);try{await rpc("aria_autonomy_recover_stale_missions",{p_stale_after:"00:02:00"});const mission=typeof body?.mission_id==="string"?await rpc("aria_mission_claim_by_id_lease",{p_mission_id:body.mission_id,p_worker_id:V,p_lease_for:"00:05:00"}):await rpc("aria_mission_claim_next_lease",{p_worker_id:V,p_lease_for:"00:05:00"});if(!mission)return out({ok:true,status:"idle",runtime:V});const id=String(mission.mission_id),goal=String(mission.goal||"");const recalled=await recall(goal,token);const context={version:"cognitive-loop-v2",available:recalled.available,recall_count:recalled.results.length,memory_ids:recalled.results.map((x:any)=>x.memory_id||x.id).filter(Boolean)};await emit(id,"cognitive_recall_completed",context);const steps=Array.isArray(mission.checkpoint?.plan)&&mission.checkpoint.plan.length?mission.checkpoint.plan:await plan(goal,context,token);if(!Array.isArray(steps)||!steps.length)throw new Error("planner_empty_steps");for(const step of steps)validateStep(step);const done=new Set<string>(Array.isArray(mission.checkpoint?.completed_steps)?mission.checkpoint.completed_steps.map(String):[]);const attempts:Record<string,number>=mission.checkpoint?.attempts&&typeof mission.checkpoint.attempts==="object"?{...mission.checkpoint.attempts}:{};const checkpoint={...(mission.checkpoint||{}),cognitive_context:context,cognitive_loop:{version:"cognitive-loop-v2",recalled_before_planning:true},plan:steps,completed_steps:[...done],attempts};await update(id,{status:"running",total_steps:steps.length,current_step:done.size,completed_steps:done.size,checkpoint});while(done.size<steps.length){const b=batch(steps,done);if(!b.length)throw new Error("dependencies_unsatisfied");await emit(id,"step_batch_started",{step_ids:b.map((s:any)=>String(s.id)),executor_types:b.map((s:any)=>targetType(s)),parallel:b.length>1});const results=await Promise.all(b.map(async(step:any)=>{const sid=String(step.id);attempts[sid]=Number(attempts[sid]||0)+1;const attempt=attempts[sid];await emit(id,"step_started",{step_id:sid,operation:step.operation,executor_type:targetType(step),attempt});try{const result=await executeStep(id,step,attempt,token);const passed=result?.status==="succeeded"||result?.ok===true;if(passed)await emit(id,"step_succeeded",{step_id:sid,executor_type:result.executor_type,operation:result.operation||step.operation,attempt});else await emit(id,"step_failed",{step_id:sid,executor_type:result.executor_type,operation:result.operation||step.operation,attempt,status:result.status});return{step,result,passed}}catch(e){const reason=e instanceof Error?e.message:String(e);await emit(id,"step_failed",{step_id:sid,executor_type:targetType(step),operation:step.operation,attempt,reason});return{step,result:{status:"failed",executor_type:targetType(step),operation:step.operation,error:{code:"executor_error",message:reason}},passed:false}}}));const failures=results.filter((x:any)=>!x.passed);for(const x of results)if(x.passed)done.add(String(x.step.id));await update(id,{current_step:done.size,completed_steps:done.size,next_action:failures.length?"recovery: step failure":(done.size<steps.length?"next_ready_batch":"verify_goal"),checkpoint:{...checkpoint,completed_steps:[...done],attempts,last_batch:b.map((s:any)=>String(s.id)),last_executor_types:b.map((s:any)=>targetType(s))}});if(failures.length)throw new Error(`step_failed:${String(failures[0].step.id)}`)}await emit(id,"mission_verified",{steps:steps.length,completed_steps:done.size,executor_types:[...new Set(steps.map((s:any)=>targetType(s)))],verified:true});await update(id,{status:"succeeded",current_step:steps.length,total_steps:steps.length,completed_steps:steps.length,next_action:null,lease_owner:null,lease_until:null,finished_at:new Date().toISOString(),checkpoint:{...checkpoint,completed_steps:[...done],attempts,model_execution_verified:steps.some((s:any)=>targetType(s)==="model"),universal_execution_verified:true,executor_types:[...new Set(steps.map((s:any)=>targetType(s)))]}});return out({ok:true,status:"succeeded",mission_id:id,runtime:V,executor_types:[...new Set(steps.map((s:any)=>targetType(s)))],results:done.size})}catch(e){const reason=e instanceof Error?e.message:String(e);const id=typeof body?.mission_id === "string"?body.mission_id:null;if(id){try{await update(id,{status:"paused",next_action:"recovery: universal runner exception",last_stderr:reason,lease_owner:null,lease_until:null})}catch{}}return out({ok:false,status:"paused",mission_id:id,runtime:V},200)}});
