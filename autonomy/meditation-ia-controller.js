'use strict';
const fs=require('node:fs');const fsp=fs.promises;const path=require('node:path');
const VERSION='aria-meditation-ia-v1';const MODES=Object.freeze(['standby','active','paused','stopped']);const COMMANDS=Object.freeze(['START','AVANZA','CONTINUA','PAUSA','CERRAR','DETENER','ESTADO']);
function fingerprintMission(value){const raw=typeof value==='string'?value:JSON.stringify(value||{});return raw.toLowerCase().replace(/\s+/g,' ').trim().slice(0,500)}
function defaultState(){return{version:VERSION,mode:'standby',session_id:null,tick_count:0,active_mission_id:null,active_goal:null,started_at:null,paused_at:null,stopped_at:null,last_tick_at:null,last_result:null,last_error:null,last_checkpoint:null,failure_ledger:{},command_offset:0}}
function createFileStateStore({root_dir}={}){if(!root_dir)throw new TypeError('root_dir required');const root=path.resolve(root_dir),statePath=path.join(root,'state.json'),lockPath=path.join(root,'controller.lock');async function ensureRoot(){await fsp.mkdir(root,{recursive:true})}async function load(){await ensureRoot();try{return{...defaultState(),...JSON.parse(await fsp.readFile(statePath,'utf8'))}}catch{const s=defaultState();await save(s);return s}}async function save(state){await ensureRoot();const tmp=`${statePath}.${process.pid}.tmp`;await fsp.writeFile(tmp,JSON.stringify(state,null,2),'utf8');await fsp.rename(tmp,statePath);return state}async function acquireLock(){await ensureRoot();try{const h=await fsp.open(lockPath,'wx');await h.writeFile(JSON.stringify({pid:process.pid,acquired_at:new Date().toISOString()}),'utf8');await h.close();return true}catch(e){if(e.code!=='EEXIST')throw e;try{const old=JSON.parse(await fsp.readFile(lockPath,'utf8'));if(old?.pid&&old.pid!==process.pid){try{process.kill(Number(old.pid),0);return false}catch{await fsp.rm(lockPath,{force:true});const h=await fsp.open(lockPath,'wx');await h.writeFile(JSON.stringify({pid:process.pid,acquired_at:new Date().toISOString(),reclaimed:true}),'utf8');await h.close();return true}}}catch{}return false}}async function releaseLock(){await fsp.rm(lockPath,{force:true})}return Object.freeze({root,statePath,lockPath,load,save,acquireLock,releaseLock})}
function createMeditationController({stateStore,commandSource,log,requestTick,checkpoint=async record=>({status:'local_only',record}),isUserIdle=async()=>true,ensureNotepad=async()=>{},onModeChange=async()=>{},onMissionEvent=async()=>{},heartbeat_ms=30000,min_idle_seconds=45,max_consecutive_failures=3,now=()=>new Date()}={}){if(!stateStore||typeof stateStore.load!=='function')throw new TypeError('stateStore required');for(const [n,f] of Object.entries({read:commandSource?.read,log,requestTick,checkpoint,isUserIdle,ensureNotepad,onModeChange,onMissionEvent}))if(typeof f!=='function')throw new TypeError(`${n} function required`);if(!Number.isInteger(heartbeat_ms)||heartbeat_ms<10000)throw new TypeError('heartbeat_ms must be >= 10000');let state=defaultState(),timer=null,busy=false,locked=false;const persist=async()=>stateStore.save(state);const status=()=>Object.freeze({...state,failure_ledger:{...state.failure_ledger}});const write=message=>log(`[${VERSION}] ${message}`);const setMode=mode=>{if(!MODES.includes(mode))throw new Error(`invalid_mode:${mode}`);state={...state,mode}};
async function handleCommand(command){const c=String(command||'').trim().toUpperCase();if(!COMMANDS.includes(c))return{status:'ignored',command:c||null};if(c==='START'||c==='AVANZA'||c==='CONTINUA'){if(state.mode==='active')return{status:'already_active',command:c};if(!locked){locked=await stateStore.acquireLock();if(!locked)return{status:'locked',reason:'another_meditation_controller_is_active'}}setMode('active');state={...state,session_id:state.session_id||`med-${now().getTime()}-${process.pid}`,started_at:state.started_at||now().toISOString(),paused_at:null,stopped_at:null};await persist();await ensureNotepad();await write(`MODE ACTIVE command=${c}`);await onModeChange(status());if(!timer)timer=setInterval(()=>{void tick('heartbeat')},heartbeat_ms);return{status:'resumed',command:c}}if(c==='PAUSA'){setMode('paused');state={...state,paused_at:now().toISOString()};await persist();await write('MODE PAUSED command=PAUSA');await onModeChange(status());return{status:'paused',command:c}}if(c==='CERRAR'||c==='DETENER'){setMode('stopped');state={...state,stopped_at:now().toISOString()};await persist();const cp=await checkpoint({type:'stop',reason:c,state:status(),session_id:state.session_id});state={...state,last_checkpoint:cp};await persist();await write(`MODE STOPPED command=${c}`);await onModeChange(status());return{status:'stopped',command:c,checkpoint:cp}}await write(`STATUS ${JSON.stringify(status())}`);return{status:'reported',command:c}}
async function consumeCommands(){const r=await commandSource.read(state.command_offset||0);let next=state.command_offset||0;for(const e of r?.entries||[]){next=Math.max(next,Number(e.next_offset||next));const result=await handleCommand(e.command);if(result.status==='stopped')break}if(next!==state.command_offset){state={...state,command_offset:next};await persist()}return r?.entries||[]}
async function tick(reason='scheduled'){if(busy)return{status:'busy',reason};busy=true;state={...state,tick_count:state.tick_count+1,last_tick_at:now().toISOString()};await persist();try{const entries=await consumeCommands();if(state.mode!=='active')return{status:'inactive',mode:state.mode,commands:entries.length};if(!(await isUserIdle())&&min_idle_seconds>0){await write(`USER_PRESENT reason=${reason} execution_deferred=true`);return{status:'deferred_user_present',tick:state.tick_count}}await ensureNotepad();const response=await requestTick({state:status(),reason,tick:state.tick_count});state={...state,last_result:response};if(response?.mission_created){state={...state,active_mission_id:response.mission_created,active_goal:response.goal||null};await onMissionEvent({type:'mission_created',...response});await write(`MISSION CREATED id=${response.mission_created} goal=${JSON.stringify(response.goal||'')}`)}else if(response?.active_mission_id){state={...state,active_mission_id:response.active_mission_id,active_goal:response.goal||null}}else if(response?.status==='idle')await write(`IDLE tick=${state.tick_count}`);const failed=response?.status==='failed'||response?.ok===false;if(failed){const key=fingerprintMission(response?.goal||response?.mission_created||response);const prev=state.failure_ledger[key]||{consecutive:0,total:0};const f={consecutive:prev.consecutive+1,total:prev.total+1,last_error:response?.error||response?.status,last_at:now().toISOString()};state={...state,failure_ledger:{...state.failure_ledger,[key]:f},last_error:f.last_error};await persist();await write(`FAILURE consecutive=${f.consecutive} total=${f.total}`);if(f.consecutive>=max_consecutive_failures){await write('FAILURE_THRESHOLD reached=3 action=alternate_strategy_requested');await onMissionEvent({type:'fallback_requested',consecutive_failures:f.consecutive,mission:response,reason:'same_goal_failed_3_times'})}}const cp=await checkpoint({type:'tick',reason,state:status(),response});state={...state,last_checkpoint:cp};await persist();return{status:'completed',tick:state.tick_count,response,checkpoint:cp}}catch(error){state={...state,last_error:String(error?.message||error),last_result:{status:'failed',error:String(error?.message||error)}};await persist();await write(`ERROR ${state.last_error}`);return{status:'failed',tick:state.tick_count,error:state.last_error}}finally{busy=false}}
async function start(){if(!locked){locked=await stateStore.acquireLock();if(!locked)return{status:'locked',reason:'another_meditation_controller_is_active'}}state=await stateStore.load();setMode('active');state={...state,session_id:`med-${now().getTime()}-${process.pid}`,started_at:now().toISOString(),stopped_at:null,paused_at:null};await persist();await ensureNotepad();await write(`START session=${state.session_id} heartbeat_ms=${heartbeat_ms}`);await onModeChange(status());if(!timer)timer=setInterval(()=>{void tick('heartbeat')},heartbeat_ms);void tick('startup');return{status:'started',state:status()}}async function pause(){return handleCommand('PAUSA')}async function resume(){return handleCommand('AVANZA')}async function stop(){const r=await handleCommand('CERRAR');if(timer)clearInterval(timer);timer=null;if(locked){locked=false;await stateStore.releaseLock()}return r}async function shutdown(){if(timer)clearInterval(timer);timer=null;if(locked){locked=false;await stateStore.releaseLock()}}return Object.freeze({version:VERSION,start,pause,resume,stop,shutdown,tick,status:()=>status(),handleCommand,consumeCommands})}
module.exports=Object.freeze({VERSION,MODES,COMMANDS,fingerprintMission,createFileStateStore,createMeditationController});
