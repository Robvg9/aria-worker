{
  "version": "aria-tool-mcp-gateway-v1.1.0",
  "mission": "11.1",
  "kind": "tool_mcp_gateway",
  "mode": "controlled_runtime",
  "live_dispatch": false,
  "controlled_dispatch": true,
  "interface_types": ["mcp", "api"],
  "consumes": ["tool_registry", "governance_decision", "tool_adapter"],
  "does_not_own": [
    "tool_discovery",
    "tool_selection",
    "approval_creation",
    "credential_resolution",
    "quota",
    "capacity",
    "fallback",
    "canonical_memory"
  ],
  "rules": [
    "unknown tool status never becomes available",
    "unknown operation never becomes allowed",
    "selection never implies authorization",
    "authorization is bound to execution, request, tool, operation and risk class",
    "high-risk and destructive operations require human verification when required by governance",
    "plaintext passwords and credentials are never accepted or stored",
    "results are normalized and sanitized",
    "controlled dispatch requires an injected adapter",
    "gateway creates no network client and does no external I/O itself",
    "live external dispatch remains disabled by default"
  ]
}
