'use strict';

const fs = require('fs');

const file = process.argv[2] || 'supabase/functions/aria-mission-runner-v22/index.ts';
const source = fs.readFileSync(file, 'utf8');

if (source.includes('const EAS_API =')) {
  console.log('EAS runner patch already present');
  process.exit(0);
}

const constantsNeedle = 'const AGENT = `${URL}/functions/v1/aria-agent-runtime-v1`;';
const constantsPatch = `${constantsNeedle}\nconst EAS_API = 'https://api.expo.dev';\nconst EAS_TOKEN = Deno.env.get('EXPO_TOKEN') ?? '';\nconst EAS_PROJECT_ID = '1b23b091-f7b6-4dc2-b328-c8e5ec07de57';`;

let output = source.replace(constantsNeedle, constantsPatch);
if (output === source) throw new Error('EAS constants anchor not found');

const validateNeedle = 'if (!["connector", "device", "model", "agent"].includes(type)) {';
const validatePatch = 'if (!["connector", "device", "model", "agent", "eas"].includes(type)) {';
output = output.replace(validateNeedle, validatePatch);
if (output === source) throw new Error('EAS validate anchor not found');

const validateAgentNeedle = 'if (type === "agent" && !step.target?.agent_id) throw new Error("agent_target_missing");';
const validateAgentPatch = `${validateAgentNeedle}\n  if (type === "eas" && String(step.target?.project_id || '') !== EAS_PROJECT_ID) throw new Error("eas_project_target_mismatch");`;
output = output.replace(validateAgentNeedle, validateAgentPatch);

const executeNeedle = `async function executeStep(missionId: string, step: any, token: string | null) {\n  validateStep(step);\n  const type = executorType(step);\n  if (type === "connector") return connectorExecute(missionId, step);\n  if (type === "device") return deviceExecute(missionId, step);\n  if (type === "model") return modelExecute(missionId, step, token);\n  if (type === "agent") return agentExecute(missionId, step, token);\n  throw new Error(\`unknown_executor_type:\${type}\`);\n}`;
const executePatch = `async function easGraphQL(query: string, variables: Record<string, unknown> = {}) {\n  if (!EAS_TOKEN) throw new Error("eas_credential_unavailable");\n  const response = await fetch(\`${'${EAS_API}'}/graphql\`, { method: "POST", headers: { authorization: \`Bearer \${EAS_TOKEN}\`, "content-type": "application/json" }, body: JSON.stringify({ query, variables }) });\n  const text = await response.text();\n  let body: any;\n  try { body = JSON.parse(text); } catch { body = { raw: text.slice(0, 12000) }; }\n  if (!response.ok || body?.errors?.length) throw new Error(\`eas_graphql_\${response.status}\`);\n  return body;\n}\n\nasync function easRest(path: string, init: RequestInit = {}) {\n  if (!EAS_TOKEN) throw new Error("eas_credential_unavailable");\n  const response = await fetch(\`${'${EAS_API}'}\${path}\`, { ...init, headers: { authorization: \`Bearer \${EAS_TOKEN}\`, "content-type": "application/json", ...(init.headers || {}) } });\n  const text = await response.text();\n  let body: any;\n  try { body = JSON.parse(text); } catch { body = { raw: text.slice(0, 12000) }; }\n  if (!response.ok) throw new Error(\`eas_http_\${response.status}\`);\n  return body;\n}\n\nasync function easExecute(step: any) {\n  const operation = String(step.operation);\n  const projectId = String(step.target.project_id);\n  if (projectId !== EAS_PROJECT_ID) throw new Error("eas_project_target_mismatch");\n  if (operation === "eas.connection_status") {\n    return { ...await easGraphQL('query App($appId: String!) { app { byId(appId: $appId) { id name slug ownerAccount { id name } } } }', { appId: projectId }), status: "succeeded", executor_type: "eas", operation, project_id: projectId };\n  }\n  if (operation === "eas.workflow_definitions") {\n    return { ...await easGraphQL('query Workflows($appId: String!) { app { byId(appId: $appId) { id workflows { id name fileName createdAt updatedAt revisionsPaginated(first: 1) { edges { node { id blobSha commitSha createdAt yamlConfig } } } } } } }', { appId: projectId }), status: "succeeded", executor_type: "eas", operation, project_id: projectId };\n  }\n  if (operation === "eas.workflow_list") {\n    const limit = Math.max(1, Math.min(Number(step.input?.limit) || 20, 100));\n    const filterStatus = typeof step.input?.status === "string" ? step.input.status : null;\n    return { ...await easGraphQL('query Runs($appId: String!, $status: WorkflowRunStatus, $limit: Int!) { app { byId(appId: $appId) { id workflowRunsPaginated(first: $limit, filter: { status: $status }) { edges { node { id status gitCommitMessage gitCommitHash requestedGitRef triggeringLabelName triggerEventType createdAt updatedAt errors { title message } workflow { id name fileName } } } } } } }', { appId: projectId, status: filterStatus, limit }), status: "succeeded", executor_type: "eas", operation, project_id: projectId };\n  }\n  if (operation === "eas.workflow_info") {\n    return { data: await easRest(\`/v2/workflows/runs/\${encodeURIComponent(String(step.input?.runId || ''))}\`), status: "succeeded", executor_type: "eas", operation, project_id: projectId };\n  }\n  if (operation === "eas.workflow_dispatch") {\n    if (step.confirm !== true && step.input?.confirm !== true) return { status: "blocked", executor_type: "eas", operation, project_id: projectId, error: { code: "confirmation_required", message: "workflow dispatch requires confirm=true" } };\n    const gitRef = String(step.input?.gitRef || '');\n    const fileName = String(step.input?.fileName || '');\n    if (!gitRef || !fileName) throw new Error("eas_dispatch_input_missing");\n    return { data: await easRest('/v2/workflows/dispatch', { method: 'POST', body: JSON.stringify({ appId: projectId, gitRef, fileName, ...(step.input?.inputs ? { inputs: step.input.inputs } : {}) }) }), status: "succeeded", executor_type: "eas", operation, project_id: projectId };\n  }\n  if (operation === "eas.build_list") {\n    const limit = Math.max(1, Math.min(Number(step.input?.limit) || 20, 50));\n    const offset = Math.max(0, Number(step.input?.offset) || 0);\n    return { ...await easGraphQL('query Builds($appId: String!, $offset: Int!, $limit: Int!) { app { byId(appId: $appId) { id builds(offset: $offset, limit: $limit) { id status platform error { errorCode message docsUrl } artifacts { buildUrl applicationArchiveUrl buildArtifactsUrl xcodeBuildLogsUrl } logFiles buildProfile appIdentifier sdkVersion appVersion appBuildVersion gitCommitHash gitCommitMessage createdAt updatedAt completedAt expirationDate } } } }', { appId: projectId, offset, limit }), status: "succeeded", executor_type: "eas", operation, project_id: projectId };\n  }\n  if (operation === "eas.build_info") {\n    const buildId = String(step.input?.buildId || '');\n    if (!buildId) throw new Error("eas_build_id_required");\n    return { ...await easGraphQL('query Build($buildId: ID!) { builds { byId(buildId: $buildId) { id status platform error { errorCode message docsUrl } artifacts { buildUrl applicationArchiveUrl buildArtifactsUrl xcodeBuildLogsUrl } logFiles buildProfile distribution appIdentifier sdkVersion appVersion appBuildVersion gitCommitHash gitCommitMessage createdAt updatedAt completedAt expirationDate } } }', { buildId }), status: "succeeded", executor_type: "eas", operation, project_id: projectId };\n  }\n  if (operation === "eas.build_logs") {\n    const buildId = String(step.input?.buildId || '');\n    if (!buildId) throw new Error("eas_build_id_required");\n    const info = await easGraphQL('query Build($buildId: ID!) { builds { byId(buildId: $buildId) { id status logFiles } } }', { buildId });\n    const build = info?.data?.builds?.byId ?? null;\n    const urls = Array.isArray(build?.logFiles) ? build.logFiles : [];\n    const logs = [];\n    for (const url of urls.slice(0, 10)) { const response = await fetch(url); const text = await response.text(); logs.push({ url, ok: response.ok, status: response.status, content: text.slice(-50000) }); }\n    return { build, logs, status: "succeeded", executor_type: "eas", operation, project_id: projectId };\n  }\n  throw new Error(\`eas_operation_not_supported:\${operation}\`);\n}\n\n${executeNeedle}`;
if (!output.includes(executeNeedle)) throw new Error('EAS execute anchor not found');
output = output.replace(executeNeedle, executePatch);

fs.writeFileSync(file, output);
console.log(`Patched ${file} with EAS executor support`);
