name = "aria"
main = "worker.js"
compatibility_date = "2026-09-05"

[triggers]
crons = ["* * * * *"]

# OAuth proxy deployment sync checkpoint
# OAuth HTML response hardening deployment trigger

[assets]
directory = "./pwa/dist"
binding = "ARIA_PWA_ASSETS"
run_worker_first = true
